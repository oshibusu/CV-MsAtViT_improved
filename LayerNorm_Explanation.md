# Layer Normalization におけるアフィン変換パラメータ ($\gamma, \beta$) の役割について

正規化層（Layer Normalization, Batch Normalization等）には、通常「**正規化 (Normalization)**」と「**再スケーリング (Rescaling / Affine Transformation)**」という2つの役割があります。

## 1. 処理の流れ

入力データ $x$（ここでは振幅 $|z|$）に対して以下の処理が行われます。

### Step 1: 正規化 (Normalization)
まず、データの統計的な偏りをなくし、整った分布に変換します。
今回の場合、「平均を維持し、分散のみを1にする」という特殊な正規化を行うため、以下のようになります。

$$ \hat{x} = \frac{x - \mu}{\sigma} + \mu $$

*   $\mu$: 入力データ $x$ の平均
*   $\sigma$: 入力データ $x$ の標準偏差
*   $\hat{x}$: 正規化されたデータ（平均は $\mu$ のまま、分散は 1）

### Step 2: アフィン変換 (Affine Transformation)
次に、学習可能なパラメータ $\gamma$ (スケール) と $\beta$ (シフト) を用いて、分布を微調整します。

$$ y = \hat{x} \cdot \gamma + \beta $$

*   $\gamma$: 分散をどの程度広げるか（初期値は通常 1.0）
*   $\beta$: 平均をどの程度ずらすか（初期値は通常 0.0）
*   $y$: 最終的な出力

---

## 2. パラメータ ($\gamma, \beta$) の有無による違い

振幅正規化において、これらのパラメータを採用するかどうかで、ネットワークの挙動が以下のように変わります。

### パターンA: パラメータあり（標準的）
$$ y = (\text{正規化後の振幅}) \cdot \gamma + \beta $$

*   **メリット**:
    *   **柔軟性が高い**: ネットワークが学習を通じて、「分散1」が最適でないと判断した場合、自分で最適な分散 ($\gamma$) やバイアス ($\beta$) を見つけることができます。
    *   **表現力の回復**: 強制的な正規化によって失われるかもしれない「データの表現力」を取り戻すことができます。
*   **振幅における注意点**:
    *   $\beta$ が大きな負の値になると、計算結果としての振幅が負になるリスクがあります（ただし後のReLU等でカットされれば問題ない場合も多いです）。

### パターンB: パラメータなし（純粋な正規化のみ）
$$ y = \text{正規化後の振幅} $$

*   **メリット**:
    *   **物理的な意味が明確**: 「分散は必ず1である」という制約が常に保証されます。
    *   **安定性**: 余計なパラメータがないため、振幅のスケールが暴れることがありません。
*   **デメリット**:
    *   **制約が強い**: ネットワークの後の層が「やっぱり分散は10くらい欲しい」と思っても、変える手段がありません。

## 3. 今回のケース（振幅正規化）での推奨

今回は「位相を保持し、振幅のみを正規化する」という特殊なケースです。また、「平均を維持する」という要件も加わっています。

*   **推奨: パターンA（パラメータあり）**
    *   理由: Deep Learningにおいては、制約を強くしすぎるよりも、ネットワークに調整の自由度（$\gamma, \beta$）を与えた方が精度が高くなる傾向があります。初期状態では $\gamma=1, \beta=0$ からスタートするため、学習初期は「純粋な正規化」として振る舞い、必要に応じてネットワークが自分で調整していく形になります。

もし「振幅という意味を厳密に守りたい（勝手なシフトを許したくない）」場合は、$\beta$ だけ削除して $\gamma$ のみを残す、あるいは両方削除する、という選択肢もあります。
