# 研究進捗報告 (2026/01/23)

### 1. Amplitude Layer Normalization の改良

振幅正規化レイヤー `AmplitudeLayerNormalization` の正規化ロジックを改良しました。
従来は振幅の分布を強制的に「平均0・分散1」にしていましたが、振幅（信号強度）の平均値を維持することが重要であると考え、**「振幅の平均値を保持しつつ、分散のみを1に正規化する」** 手法に変更しました。

**数式**:
入力の振幅 $|z|$、その平均を $\mu$、標準偏差を $\sigma$ とします。

$$
z' = (\frac{|z| - \mu}{\sigma + \epsilon} + \mu) \cdot \gamma + \beta
$$

*   前半の項 $(\frac{|z| - \mu}{\sigma} + \mu)$ により、平均は入力と同じ $\mu$ に保たれ、分散は 1 に正規化されます。
*   後半で学習可能なパラメータ $\gamma$ (スケール) と $\beta$ (シフト) を適用し、ネットワークの表現力を確保します。

これにより、振幅が持つ物理的な意味（信号強度の強弱）を損なうことなく、統計的なバラツキを抑制できるようになります。
本機能はデフォルトで有効（`--layer-norm-type amplitude`）です。

