# 研究進捗報告 (2026/01/20)

### 実装変更点

#### 1. 振幅正規化 (Amplitude Layer Normalization) の実装
従来の実部・虚部を独立に正規化する手法 (`Split LayerNorm`) では、正規化によって位相情報（偏角）が歪んでしまうという課題がありました。
そこで、**振幅（信号強度）のみを正規化し、位相情報を完全に保持する** 新しい正規化レイヤー `AmplitudeLayersNormalization` を実装・導入しました。

**数式**:
入力 $z = |z|e^{j\phi}$ に対して、
$$
z' = \text{LayerNorm}(|z|) \cdot e^{j\phi}
$$
ここで、$\text{LayerNorm}$ は振幅 $|z|$ の分布を平均0・分散1に正規化します。

#### 2. 正規化手法の切り替えオプション追加
実験比較のため、コマンドライン引数で正規化手法を切り替えられるようにしました。

*   **`--layer-norm-type amplitude`** (デフォルト): 新手法。振幅のみ正規化。
*   **`--layer-norm-type split`**: 従来手法。実部と虚部を独立に正規化。

実行例:
```bash
python main_CV_MsAtViT.py --dataset SF --layer-norm-type amplitude
```

#### 3. 分類マップ画像の自動生成
学習完了時に、推論結果の分類マップを画像 (`.png`) として `results/plots` フォルダに自動保存する機能を追加しました。これにより、MATLAB等を開かなくても視覚的な評価が可能になります。

#### 4. その他修正
*   デフォルトデータセットを `SF` (San Francisco) に変更しました。
*   `net_flops.py` で計算時にシェイプが `None` の場合にエラーが発生するバグを修正しました。

### コード変更箇所詳細

| ファイル名 | 変更内容 | 変更箇所 (概算) |
| :--- | :--- | :--- |
| **`SAR_utils.py`** | `AmplitudeLayerNormalization` クラスの追加<br>`save_classification_map` 関数の追加 | 末尾に追加 (L250付近〜) |
| **`model_factory.py`** | `AmplitudeLayerNormalization` のインポート<br>`cmplx_ViT` 内での条件分岐による `AmplitudeLayerNormalization` 適用<br>`build_msatvit` の引数に `layer_norm_type` 追加 | L14, L123-153, L168, L198, L227 |
| **`main_CV_MsAtViT.py`** | `--layer-norm-type` 引数の追加<br>デフォルトデータセットの `SF` への変更<br>`save_classification_map` の呼び出し追加 | L293, L295-300, L516-521 |
| **`net_flops.py`** | `None` シェイプに対する例外処理の追加 | L46付近 |
